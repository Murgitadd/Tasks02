Console.WriteLine("Enter a number:");
int num = int.Parse(Console.ReadLine());

int high = highDig(num);
int low = lowDig(num);

int sum = highDig + lowDig;

Console.WriteLine(sum);

int highDig(int num)
{
    int hig = lowDig;
    
    while (num > 0)
    {

    }
}
